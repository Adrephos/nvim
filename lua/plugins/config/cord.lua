require("cord").setup({
  editor = {
    client = "neovim",
    tooltip = "北海道",
    icon = nil,
  },
  display = {
    theme = "default",
    flavor = "dark",
    view = "full",
    swap_fields = false,
    swap_icons = false,
  },
  idle = { enabled = false },
})
